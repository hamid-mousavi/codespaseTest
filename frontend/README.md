# Frontend - Coop Portal (React + Vite)

این پوشه اسکلت فرانت‌اند را دارد: React + TypeScript + Vite + Redux Toolkit + Material UI.

برای اجرا در حالت توسعه:

```bash
cd frontend
npm install
npm run dev
```

توسعه‌دهنده‌ها می‌توانند از `src` شروع کنند: `pages`, `components`, `store`, `services`.
